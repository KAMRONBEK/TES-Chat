/** Approximate height of the bottom tab bar (labels + icons); used to pad master column content on wide web. */
export const BOTTOM_TAB_BAR_HEIGHT = 56;
/** Thinner than `StyleSheet.hairlineWidth` (often 1) on web for a light top edge. */
export const BOTTOM_TAB_BAR_TOP_BORDER = 0.5;
